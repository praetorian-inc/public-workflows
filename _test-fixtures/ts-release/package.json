{
  "name": "@praetorian-inc/ts-release-test-fixture",
  "version": "0.0.1",
  "description": "Fixture exercised by test-ts-release.yml via a dry-run publish. Declares one tiny public dependency (ms) so the self-test proves `npm ci` resolves a real public dep through setup-node's default registry config.",
  "license": "UNLICENSED",
  "private": false,
  "publishConfig": {
    "access": "restricted",
    "registry": "https://npm.pkg.github.com"
  },
  "type": "module",
  "dependencies": {
    "ms": "2.1.3"
  },
  "bin": {
    "ts-release-test-fixture": "./dist/index.js"
  },
  "exports": {
    ".": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "mkdir -p dist && cp src/index.js dist/index.js",
    "test": "node --input-type=module -e \"import('./src/index.js').then(m => { if (m.hello() !== 'ok') process.exit(1) })\""
  },
  "engines": {
    "node": ">=20"
  }
}
